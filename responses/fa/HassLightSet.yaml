language: fa
responses:
  intents:
    HassLightSet:
      brightness: "روشنایی {{ slots.name }} روی {{ slots.brightness }} تنظیم شد"
      brightness_area: "روشنایی {{ slots.area }} روی {{ slots.brightness }} تنظیم شد"
      color: "رنگ {{ slots.name }} تنظیم شد"
      color_area: "رنگ {{ slots.area }} تنظیم شد"
      temperature: "دمای رنگ {{ slots.name }} تنظیم شد"
