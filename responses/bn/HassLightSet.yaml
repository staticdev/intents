language: bn
responses:
  intents:
    HassLightSet:
      brightness: "{{ slots.name }}-এর উজ্জ্বলতা {{ slots.brightness }} করা হয়েছে"
      brightness_area: "{{ slots.area }}-এর উজ্জ্বলতা {{ slots.brightness }} করা হয়েছে"
      color: "{{ slots.name }}-এর রং {{ slots.color }} করা হয়েছে"
      color_area: "{{ slots.area }}-এর রং {{ slots.color }} করা হয়েছে"
      temperature: "{{ slots.name }}-এর রঙের তাপমাত্রা সেট করা হয়েছে"
