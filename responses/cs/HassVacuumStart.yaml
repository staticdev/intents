language: cs
responses:
  intents:
    HassVacuumStart:
      default: >
        {% if slots.name is defined and slots.name %}
          Spouštím {{ slots.name }}
        {% else %}
          Spouštím vysavač
        {% endif %}
