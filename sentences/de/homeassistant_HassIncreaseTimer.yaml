language: de
intents:
  HassIncreaseTimer:
    data:
      - sentences:
          - "verlängere[ (den|meinen|unseren)] Timer um <timer_duration>"
          - "verlängere[ (den|meinen|unseren)] <timer_start> Timer um <timer_duration>"
          - "verlängere[ (den|meinen|unseren)] Timer für <timer_start> um <timer_duration>"
          - "verlängere[ (den|meinen|unseren)] {area} Timer um <timer_duration>"
          - "verlängere[ (den|meinen|unseren)] Timer <area> um <timer_duration>"
          - "verlängere[ (den|meinen|unseren)] {timer_name:name} Timer um <timer_duration>"
          - "verlängere[ (den|meinen|unseren)] Timer (namens|für) {timer_name:name} um <timer_duration>"
          - "[(den|meinen|unseren) ]Timer um <timer_duration> verlängern"
          - "[(den|meinen|unseren) ]<timer_start> Timer um <timer_duration> verlängern"
          - "[(den|meinen|unseren) ]Timer für <timer_start> um <timer_duration> verlängern"
          - "[(den|meinen|unseren) ]{area} Timer um <timer_duration> verlängern"
          - "[(den|meinen|unseren) ]Timer <area> um <timer_duration> verlängern"
          - "[(den|meinen|unseren) ]{timer_name:name} Timer um <timer_duration> verlängern"
          - "[(den|meinen|unseren) ]Timer (namens|für) {timer_name:name} um <timer_duration> verlängern"
          - "[(<timer_start>|{area}|{timer_name:name}) Timer ]<timer_duration> (mehr|länger)"
