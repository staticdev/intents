---
language: "gl"
intents:
  HassIncreaseTimer:
    data:
      - sentences:
          - "(<engadir>|<sube>) <timer_duration> [ao|o][ <meu>] <temporizador>"
          - "(<engadir>|<sube>) <timer_duration> [ao|o][ <meu>] <temporizador> [de |para ]<timer_start>"
          - "(<engadir>|<sube>) <timer_duration> [ao|o][ <meu>] <temporizador> <area>"
          - "(<engadir>|<sube>) <timer_duration> [ao|o][ <meu>] <temporizador>[ de| con nome| chamado| denominado| para] {timer_name:name}"
      - sentences:
          - "(<engadir>|<sube>) [o|ao][ <meu>] <temporizador> <timer_duration>"
          - "(<engadir>|<sube>) [o|ao][ <meu>] <temporizador> [de |para ]<timer_start> <timer_duration>"
          - "(<engadir>|<sube>) [o|ao][ <meu>] <temporizador> <area> <timer_duration>"
