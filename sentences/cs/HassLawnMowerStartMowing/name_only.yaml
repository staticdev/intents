---
# HassLawnMowerStartMowing - name_only
# example: zapni Sekačku
# slots: {name}

language: "cs"
data:
  - sentences:
      - "(nastart(uj|ovat)|<spustit>|<zapnout>) {name}"
    example: "zapni Sekačku"
    name_domains:
      - "lawn_mower"
    response: "default"
