language: es
intents:
  HassGetWeather:
    data:
      - sentences:
          - "qué tiempo hace"
          - "cómo está el tiempo"
      - sentences:
          - "qué tiempo hace en <name>"
          - "cómo está el tiempo en <name>"
          - "<dime> el tiempo en <name>"
        requires_context:
          domain: weather
